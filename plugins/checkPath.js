// export default ({ app }) => {
// 	// console.log(app.router)
// 	app.router.afterEach((to, from) => {
// 		if (from.path.slice(-1) === '/') {
// 			app.router.push(from.path.slice(0, -1))
// 		} else if (to.path.slice(-1) === '/') {
// 			app.router.push(to.path.slice(0, -1))
// 		}
// 	})
// }
